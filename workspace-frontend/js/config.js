/**
 * CONFIG.JS — Single source of truth for API URL.
 * Change API_BASE here whenever the backend URL changes.
 *
 * For local development:  window.API_BASE = '';         (uses relative /api/... paths)
 * For production:         window.API_BASE = 'https://branding-pirates-portal-server.onrender.com';
 */
window.API_BASE = 'https://branding-pirates-portal-server.onrender.com';
